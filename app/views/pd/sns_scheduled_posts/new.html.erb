<% content_for :title, "예약 발행 새로 만들기" %>
<div class="max-w-lg">
  <div class="mb-6">
    <%= link_to "← 예약 발행", pd_scheduled_posts_path, class: "text-primary text-sm hover:text-primary-dark" %>
    <h1 class="text-xl font-bold text-navy mt-2">새 예약 발행</h1>
  </div>
  <div class="bg-white rounded-lg border border-gray-100 shadow-sm p-6">
    <%= render "form", scheduled_post: @scheduled_post %>
  </div>
</div>
