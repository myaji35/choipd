<% content_for :title, "히어로 이미지 추가" %>
<div class="max-w-lg">
  <div class="mb-6">
    <%= link_to "← 히어로 이미지", admin_hero_images_path, class: "text-primary text-sm hover:text-primary-dark" %>
    <h1 class="text-xl font-bold text-navy mt-2">이미지 추가</h1>
  </div>
  <div class="bg-white rounded-lg border border-gray-100 shadow-sm p-6">
    <%= render "form", hero_image: @hero_image %>
  </div>
</div>
