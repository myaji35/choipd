<% content_for :title, "회원 추가" %>
<div class="mb-6">
  <%= link_to "← 회원 목록", admin_members_path, class: "text-primary text-sm" %>
  <h1 class="text-xl font-bold text-navy mt-2">신규 회원 등록</h1>
</div>
<div class="bg-white rounded-lg border border-gray-200 shadow-sm p-6 max-w-4xl">
  <%= render "form", member: @member %>
</div>
