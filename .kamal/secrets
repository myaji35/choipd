KAMAL_REGISTRY_PASSWORD=$(gh auth token)
RAILS_MASTER_KEY=$(cat config/master.key)
# Google OAuth 2.0 — 프로젝트 루트 .env에서 직접 읽어 서버 주입
GOOGLE_OAUTH_CLIENT_SECRET=$(grep -E '^GOOGLE_OAUTH_CLIENT_SECRET=' .env | head -1 | cut -d= -f2- | tr -d '\"'\'' ')
